{
  "name": "claude-code-backend",
  "private": true,
  "version": "0.13.5",
  "type": "module",
  "scripts": {
    "dev": "node --import tsx/esm --watch src/server.ts",
    "build": "node esbuild.mjs",
    "lint": "node ./node_modules/typescript/lib/tsc.js --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "claude-code-battery": "^0.2.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.13",
    "@vitest/coverage-v8": "^4.1.1",
    "esbuild": "^0.24.0",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3",
    "vitest": "^4.1.0"
  }
}
