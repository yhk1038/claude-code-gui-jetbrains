# Tool window stripe/header display names.
# Key format: toolwindow.stripe.<id> with spaces replaced by underscores.
# The left "Claude Sessions" tool window keeps a unique id (to avoid clashing
# with the right "Claude Code" tool window) but displays as "Claude Code".
toolwindow.stripe.Claude_Sessions=Claude Code
