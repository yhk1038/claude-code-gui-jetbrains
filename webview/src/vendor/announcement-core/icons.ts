/**
 * AUTO-GENERATED by scripts/sync-announcement-core.sh — DO NOT EDIT.
 * Source of truth: www/packages/announcement-core/src (edit there, then re-run
 *   `bash ./scripts/build.sh sync-announcement-core`).
 */

/**
 * Bundled icon whitelist for announcement rendering.
 *
 * This package deliberately does NOT depend on `@heroicons/react` (no React,
 * no UI deps) — it only carries the *names* the plugin's `IconName` enum
 * (`webview/src/router/routes.ts`) already recognizes, so a renderer on
 * either side (plugin WebView, www admin) can validate a server-sent
 * `Announcement.icon` string against this whitelist and fall back to a
 * default icon when unknown. Each renderer maps these names to its own
 * concrete icon components.
 */

/** Heroicon component names eligible for use as an announcement icon. Spelling-identical to the plugin's `IconName` enum values. */
export const ANNOUNCEMENT_ICON_NAMES: readonly string[] = [
  'Cog6ToothIcon',
  'SwatchIcon',
  'ShieldCheckIcon',
  'CommandLineIcon',
  'AdjustmentsHorizontalIcon',
  'ChartBarSquareIcon',
  'InformationCircleIcon',
  'ArrowLeftIcon',
  'ArrowsRightLeftIcon',
  'ArrowUpCircleIcon',
  'ComputerDesktopIcon',
  'BellIcon',
  'LockClosedIcon',
  'HeartIcon',
] as const;

/** Friendly, human-readable label for each bundled icon name — for admin pickers etc. */
export const ANNOUNCEMENT_ICON_LABELS: Record<string, string> = {
  Cog6ToothIcon: 'Settings',
  SwatchIcon: 'Appearance',
  ShieldCheckIcon: 'Security',
  CommandLineIcon: 'CLI',
  AdjustmentsHorizontalIcon: 'Advanced',
  ChartBarSquareIcon: 'Usage',
  InformationCircleIcon: 'Info',
  ArrowLeftIcon: 'Back',
  ArrowsRightLeftIcon: 'Switch',
  ArrowUpCircleIcon: 'Update',
  ComputerDesktopIcon: 'Remote',
  BellIcon: 'Notification',
  LockClosedIcon: 'Privacy',
  HeartIcon: 'Sponsor',
};

/** Whether `icon` is a recognized bundled icon name. */
export function isKnownAnnouncementIcon(icon: string): boolean {
  return ANNOUNCEMENT_ICON_NAMES.includes(icon);
}
