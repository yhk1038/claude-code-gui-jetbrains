/**
 * AUTO-GENERATED by scripts/sync-announcement-core.sh — DO NOT EDIT.
 * Source of truth: www/packages/<pkg>/src (edit there, then re-run
 *   `bash ./scripts/build.sh sync-announcement-core`).
 */

import {
  Cog6ToothIcon,
  SwatchIcon,
  ShieldCheckIcon,
  CommandLineIcon,
  AdjustmentsHorizontalIcon,
  ChartBarSquareIcon,
  InformationCircleIcon,
  ArrowLeftIcon,
  ArrowsRightLeftIcon,
  ArrowUpCircleIcon,
  ComputerDesktopIcon,
  BellIcon,
  LockClosedIcon,
  HeartIcon,
} from '@heroicons/react/24/outline';
import { isKnownAnnouncementIcon } from '@/vendor/announcement-core';

/**
 * An icon component compatible with the Heroicons outline set. Derived from a
 * concrete Heroicon so the `forwardRef` + `title`/`titleId` prop shape matches
 * exactly (a plain `ComponentType<SVGProps>` clashes on `ref` variance).
 */
export type AnnouncementIconComponent = typeof InformationCircleIcon;

/**
 * Maps each whitelisted announcement icon name (owned by
 * `@/vendor/announcement-core`'s `ANNOUNCEMENT_ICON_NAMES`, spelling-identical to
 * the plugin's `IconName` enum) to a concrete `@heroicons/react/24/outline`
 * component. Keys are the exact server-sendable strings.
 */
const ICON_COMPONENTS: Record<string, AnnouncementIconComponent> = {
  Cog6ToothIcon,
  SwatchIcon,
  ShieldCheckIcon,
  CommandLineIcon,
  AdjustmentsHorizontalIcon,
  ChartBarSquareIcon,
  InformationCircleIcon,
  ArrowLeftIcon,
  ArrowsRightLeftIcon,
  ArrowUpCircleIcon,
  ComputerDesktopIcon,
  BellIcon,
  LockClosedIcon,
  HeartIcon,
};

/**
 * Resolves an open-ended server `Announcement.icon` string to a bundled
 * Heroicon component. Validates against the shared whitelist first and never
 * trusts the raw string as a component/SVG reference: an unrecognized (or
 * unmapped) name falls back to `InformationCircleIcon`, preventing remote
 * SVG/markup injection through this field.
 */
export function resolveAnnouncementIcon(icon: string): AnnouncementIconComponent {
  if (isKnownAnnouncementIcon(icon)) {
    const component = ICON_COMPONENTS[icon];
    if (component) return component;
  }
  return InformationCircleIcon;
}
